import React from 'react';

const Service = ({Service}) => {
    const { title, name}  = Service
    console.log(Service)
    return (
        <div>
            
        </div>
    );
};

export default Service;